server.port: 8022
spring:
  application.name: games
  profiles.active: dev
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://${auth0.domain}/
auth0:
  audience: https://auth.gamelib.com
  domain: gamelib.eu.auth0.com